# DeepSeek Harness 学习知识库 —— 总览与学习路线

> 这套文档是 Claude 在通读 deepseek-harness 源码（核心包逐文件精读 + 全仓探索）后撰写的**独立解读**，视角与官方 `deepseek-harness/docs` 互补：官方文档偏"契约与规范"，本套偏"机制拆解、源码走读与工程决策的 why"。
>
> 📌 **版本基线**：初版基于 `dsh@0.1.0-rc.5`（commit 47f9438），经 `rc.8`（141eb6f）后，现已按 **`dsh@0.1.2-alpha.2`（commit 0a53fb5）** 增量更新——各篇中标 "📌 0.1.2 增量" 的章节即本轮差异（1520 个 commit），09/10/11 三篇为全新。上一轮的 "rc.8 增量" 章节保留以对照演进。

## 一句话认识 dsh

**dsh 是一个"一切皆插件"的 agent 运行时**：模型适配器、工具注册表、会话日志、甚至 agent 循环本身都是 Cordis 插件，全部可以从配置层替换。`dsh web` 这个产品只是官方用插件拼出的一种默认组合。

## 文档目录

| 篇 | 主题 | 一句话 |
|---|---|---|
| [01](01-架构总览与启动组合.md) | 架构总览与启动组合 | 从 `dsh web` 一条命令到 200+ 包的插件树：profile → bundle → 补丁层 |
| [02](02-Cordis内核机制.md) | Cordis 内核机制 | Context/Service/Fiber/Effect/四种事件派发；waterfall 的 next() 链源码拆解 |
| [03](03-Agent循环与会话日志.md) | Agent 循环与会话日志 | turn/step 状态机、收件箱三通道、append-only 日志与 Surface 机制 |
| [04](04-工具系统与执行管线.md) | 工具系统与执行管线 | defineTool、六段管线、单调守卫、**PTC**（原 Code Mode）、客户端派生卡片 |
| [05](05-能力接缝与服务地图.md) | 能力接缝与服务地图 | seam 三角色定义、40+ ctx 服务速查、"换 provider 换世界" |
| [06](06-插件开发实战指南.md) | 插件开发实战指南 | 九类插件选型、可抄骨架、挂载三法、纪律清单、插件点子 |
| [07](07-Web客户端与外部协议.md) | Web 客户端与外部协议 | 浏览器里的 Cordis 树、Typert RPC、Chat 三段渲染、SDK/ACP 对比 |
| [08](08-AgentTeams多代理协作.md) | Agent Teams 多代理协作 | rc.8 实验特性：持久花名册/信箱/CAS 任务板，全部叠在 subagent 接缝之上 |
| [09](09-浏览器内运行时WebWorker.md) | 🆕 浏览器内运行时（WebWorker） | 整棵插件树跑进 Web Worker：唯一平台分叉是一张模块代理表，能力包零改动 |
| [10](10-Inspector与运行时检视.md) | 🆕 Inspector 与运行时检视 | 跨领域 CDP、执行领域/协议平面、Elements 里的活 Cordis 树 |
| [11](11-Webhook与自动化入口.md) | 🆕 Webhook 与自动化入口 | 两方法接缝：验签的外部事件 → 一个普通根会话，然后退出画面 |

## 推荐学习路线

**第一层：建立世界观（半天）**
1. 读 01 → 02，跑 `dsh --profile web --dump-config` 亲眼看合成树
2. 记住三条铁律：一切注册皆 effect / 模型可见 ⟺ 已落日志 / 新行为挂扩展点不改循环

**第二层：吃透心脏（一到两天）**
3. 读 03 → 04，对照源码 `packages/core/agent-loop/src/agent.ts`（~500 行，值得逐行读）
4. 读 05，扫一遍 `docs/capability-seams.md` 生成图，理解"接缝三角色"

**第三层：动手（一天）**
5. 按 06 的骨架写一个玩具工具插件，用 `--patch` 挂上跑通
6. 想做 UI/协议对接再读 07

**源码精读推荐顺序**（每个都短小精悍）：
- `vendor/cordis/src/events.ts` 的 `waterfall()` —— 20 行看懂全部策略机制
- `packages/core/session/src/types.ts` —— 事件词汇表即系统契约
- `packages/core/agent-loop/src/agent.ts` —— 心脏
- `packages/shell/`（shell 接缝三角色）—— 官方钦定的 seam 模板
- `packages/bundle/base/cordis.patch.yml` —— 产品 = 一列配置行

## 我认为最值得借鉴的五个设计决策

1. **日志优先（log-first）**：模型上下文、UI、恢复、遥测全部从一条 append-only 事件流派生。压缩用 surface `replace` 而非删除——历史永远完整。
2. **策略与能力解耦**：工具本体零策略；沙箱/权限/hook/审计横跨全部工具族。fail-closed 审批（无应答者=拒绝）。
3. **纪元驱动的生命周期**：依赖的提供者换人=纪元变化=消费者自动重载，HMR 因此"暴力但正确"。
4. **配置即产品**：web/headless 只是补丁层堆叠差异；用户任何一层都能替换任意行。浏览器前端同样由宿主配置组合，前端壳零决策。
5. **防御性版本策略**：未识别且未标 `ignorable` 的事件 → 拒绝重建会话。宁可过度拒绝，不可静默读坏。

## 与官方文档的对照关系

| 官方文档 | 本套对应 | 互补点 |
|---|---|---|
| `docs/architecture.md` | 01/03 | 官方给扩展点地图；本套给启动流水线源码级细节 |
| `docs/cordis-primer.md` + `cordis-tutorial/` | 02 | 官方教用法；本套拆 vendor 源码实现（proxy/epoch/waterfall 内部） |
| `docs/tool-execution-pipeline.md` | 04 | 官方给流程图；本套给身份保护/finalizer 快照等防御细节的 why |
| `docs/capability-seams.md`（生成） | 05 | 官方是权威全量表；本套按域分组 + 设计规范提炼 |
| `docs/cookbook/` | 06 | 官方是逐步指南；本套是选型决策表 + 纪律自查 + 点子库 |
| `docs/subsystems/`（30+ 篇） | 07 及各篇 | 官方逐包契约；本套跨包机制串讲 |
